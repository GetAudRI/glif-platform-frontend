# GitHub repository migration

**Date:** 2026-04-14

This repository was **ported from the personal GitHub account** (`kkdonn`) **to the GetAudRI organization**:

- **Canonical remote:** `https://github.com/GetAudRI/glif-platform-frontend`
- **SSH remote:** `git@github.com:GetAudRI/glif-platform-frontend.git`

From this date onward, **use the GetAudRI org repo only** for clones, issues, pull requests, and CI. Treat the old personal URL as historical; update any bookmarks, local `origin` remotes, and integrations (e.g. Railway, hosting) to point here.

**Branches:** Continue day-to-day work on the team’s active branches (e.g. `AudRI` and feature branches). Do not rely on the pre-migration personal remote for new work.
